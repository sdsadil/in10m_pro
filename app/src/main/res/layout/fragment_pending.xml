<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.activities.company_pros.pending.Pending">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_PA"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </androidx.recyclerview.widget.RecyclerView>

    <com.in10mServiceMan.widgets.textview.CustomRegularTextView
        android:id="@+id/data_PA"
        android:visibility="gone"
        android:text="No Data Found"
        android:textSize="16dp"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <ImageView
        android:id="@+id/addPendingServiceman"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:tint="@color/accept_button"
        app:srcCompat="@drawable/ic_add_circle_black_24dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>